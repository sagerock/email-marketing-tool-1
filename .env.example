# ===================================
# ENVIRONMENT VARIABLES
# (Combined Railway deployment)
# ===================================

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# API URL - leave empty for same-origin requests (combined deployment)
# Only set this if frontend and backend are on different domains
VITE_API_URL=

# Base URL for unsubscribe links and OAuth callbacks
# This should be your Railway app URL
BASE_URL=https://mail.sagerock.com

# Server Configuration
PORT=3001
NODE_ENV=production

# ===================================
# LOCAL DEVELOPMENT
# ===================================

# For local development, create a .env file with:
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
# SUPABASE_SERVICE_KEY=your-supabase-service-role-key
# VITE_API_URL=http://localhost:3001
# BASE_URL=http://localhost:5173
# PORT=3001
# NODE_ENV=development

# ===================================
# NOTES
# ===================================

# SendGrid API keys are stored in the database per client
# No need to add them here

# Salesforce credentials are also stored per-client in the database
# using the Client Credentials OAuth flow

# To get your Supabase keys:
# 1. Go to your Supabase project
# 2. Settings > API
# 3. Copy the URL and anon/service keys

# Railway will automatically set PORT
