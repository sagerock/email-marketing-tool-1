# ===================================
# ENVIRONMENT VARIABLES
# (Combined Railway deployment)
# ===================================

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co

# Frontend key (safe to expose publicly)
# New format: sb_publishable_... (recommended)
# Legacy format: eyJ... (JWT anon key - still works)
VITE_SUPABASE_ANON_KEY=sb_publishable_your-key-here

# Backend key (NEVER expose publicly!)
# New format: sb_secret_... (recommended - easier to rotate)
# Legacy format: eyJ... (JWT service_role key - still works)
SUPABASE_SERVICE_KEY=sb_secret_your-key-here

# API URL - leave empty for same-origin requests (combined deployment)
# Only set this if frontend and backend are on different domains
VITE_API_URL=

# Base URL for unsubscribe links and OAuth callbacks
# This should be your Railway app URL
BASE_URL=https://mail.sagerock.com

# Server Configuration
PORT=3001
NODE_ENV=production

# ===================================
# LOCAL DEVELOPMENT
# ===================================

# For local development, create a .env file with:
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=sb_publishable_your-key
# SUPABASE_SERVICE_KEY=sb_secret_your-key
# VITE_API_URL=http://localhost:3001
# BASE_URL=http://localhost:5173
# PORT=3001
# NODE_ENV=development

# ===================================
# NOTES
# ===================================

# SendGrid API keys are stored in the database per client
# No need to add them here

# Salesforce credentials are also stored per-client in the database
# using the Client Credentials OAuth flow

# To get your Supabase keys:
# 1. Go to your Supabase project dashboard
# 2. Settings > API > API Keys tab
# 3. Click "Create new API Keys" if needed
# 4. Copy the Publishable key for VITE_SUPABASE_ANON_KEY
# 5. Create a Secret key for SUPABASE_SERVICE_KEY
#
# Note: Legacy JWT keys (Settings > API > Legacy API Keys) still work
# but the new sb_publishable_/sb_secret_ keys are easier to rotate

# Railway will automatically set PORT
